export { createAdBlockDetector } from "./core/detector";
export {
  DEFAULT_DOM_CLASS_NAMES,
  DEFAULT_DOM_ID,
  DEFAULT_DOM_TIMEOUT_MS,
  DEFAULT_NETWORK_TIMEOUT_MS,
} from "./core/options";
export type {
  AdBlockDetector,
  AdBlockDetectorOptions,
  Detected,
  DetectionSnapshot,
  DomProbeOptions,
  NetworkProbeOptions,
} from "./core/types";
